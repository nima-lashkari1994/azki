export type VehicleType = {
    id:number,
    title:string,
    usages:VehicleModel[]
}

type VehicleModel = {
    id:number,
    title:string
}

export type Company = {
    id:number,
    title:string
}

export type Discount = {
    id:number,
    title:string
}